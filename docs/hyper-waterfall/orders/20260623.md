# 오늘 할 일 - 2026-06-23

## 현재 초점

- Milestone: M001
- Task: task_m001_002
- 제목: Public type refinement와 config normalization boundary
- 목표: Chart.js-like public config와 성능 중심 internal normalized model 사이의 첫 경계를 계획한다.
- 상태: 완료
- 다음 게이트: task_m001_003 수행계획서 작성

## 맥락

`task_m001_001`은 완료되었다.

현재 만들어진 것은 다음이다.

- pnpm workspace
- `@xelstack/chart` package skeleton
- public type entry
- 빈 internal type boundary
- colocated public type contract test

아직 만들지 않은 것은 다음이다.

- `createChart`
- config normalization 구현
- append buffer
- renderer
- scheduler
- downsampling

따라서 다음 작업은 runtime 구현이 아니라 public type과 internal normalized model 사이의 경계를 먼저 잡는 것이다.

## 대기열

| Task          | 제목                                                   | 상태 | 다음 게이트                   |
| ------------- | ------------------------------------------------------ | ---- | ----------------------------- |
| task_m001_002 | Public type refinement와 config normalization boundary | 완료 | task_m001_003 수행계획서 작성 |

## task_m001_002 예상 산출물

수행계획서:

- `docs/hyper-waterfall/plans/task_m001_002.md`

이 문서에서 결정할 것:

- `ChartOptions`를 지금 public type에 넣을지 보류할지
- `DatasetConfig.data`와 append input의 의미를 분리할지
- public package entry contract test를 이번 task에 넣을지
- internal type model의 첫 범위
- config normalization boundary의 첫 구현 범위
- validation result를 단순 union으로 둘지, 더 강한 도메인 모델로 둘지

구현계획서:

- 수행계획서 승인 후 `docs/hyper-waterfall/plans/task_m001_002_impl.md`를 작성한다.

## 오늘 하지 않는 것

- `createChart` runtime을 구현하지 않는다.
- Canvas renderer를 구현하지 않는다.
- append buffer를 구현하지 않는다.
- RAF scheduler를 구현하지 않는다.
- downsampling을 구현하지 않는다.
- style, scale, interaction option 전체를 public API로 확정하지 않는다.
- DDD/FP library를 도입하지 않는다.

## 권장 다음 행동

`task_m001_002`는 완료되었다.

다음 작업은 `task_m001_003` 수행계획서 작성이다.

다음 파일:

```text
docs/hyper-waterfall/plans/task_m001_003.md
```

## 승인

- 상태: 완료
- 승인자: user
- 날짜: 2026-06-24
