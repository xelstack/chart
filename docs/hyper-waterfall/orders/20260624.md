# 오늘 할 일 - 2026-06-24

## 현재 초점

- Milestone: M001
- Task: task_m001_003
- 제목: Import policy와 module boundary tooling
- 목표: 내부 alias, public entry, import 정렬, build/test 도구의 import 해석 경계를 정리한다.
- 상태: 완료
- 다음 게이트: commit/push

## 맥락

`task_m001_002`는 완료되었다.

완료된 것은 다음이다.

- public `InitialDataInput` 의미 분리
- internal normalized config model
- `normalizeConfig` boundary
- config normalization runtime test
- package-local `@/* -> ./src/*` alias
- Vitest alias 설정
- workspace TypeScript SDK 설정

이번 task는 새 runtime feature 구현이 아니라 import/module boundary를 정리하는 작업이다.

이유:

- `@/*` alias가 들어왔지만, 어떤 import에 써야 하는지 규칙이 아직 없다.
- public package entry와 internal module import의 경계를 문서화해야 한다.
- import 정렬 자동화는 도입하기로 했고, 세부 정렬 규칙을 구현계획서에서 정해야 한다.
- TypeScript, Vitest, tsdown build가 같은 import 정책을 이해하는지 확인해야 한다.

## 대기열

| Task          | 제목                                    | 상태 | 다음 게이트 |
| ------------- | --------------------------------------- | ---- | ----------- |
| task_m001_003 | Import policy와 module boundary tooling | 완료 | commit/push |

## 오늘 하지 않는 것

- `createChart` runtime을 구현하지 않는다.
- input normalization을 구현하지 않는다.
- append buffer를 구현하지 않는다.
- renderer를 구현하지 않는다.
- scheduler를 구현하지 않는다.
- downsampling을 구현하지 않는다.
- public subpath export를 추가하지 않는다.

## 권장 다음 행동

`task_m001_003` 변경사항을 commit/push한다.

다음 파일:

```text
git status --short
```

## 승인

- 상태: 수행계획서 승인됨, 구현계획서 승인됨, 단계 완료보고 승인됨, 최종보고서 승인됨
- 승인자: user
- 날짜: 2026-06-24
